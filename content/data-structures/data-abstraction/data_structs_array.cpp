#include <iostream>

using namespace std;

int main() {
    int numbers[5] = {10, 20, 30, 40, 50};

    cout << "All elements of the array:" << endl;
    for (int i = 0; i < 5; ++i) {
        cout << "Element at index " << i << ": " << numbers[i] << endl;
    }

    // Modifying numbers array elements, doubling their values
    // your-code-here


    // Verify that numbers array is updated
    // your-code-here

    return 0;
}
